# 3D Models

## `head.obj`
This 3D head mesh was extracted from the `standard_bem.mat` (Boundary Element Method) template provided by the [FieldTrip toolbox](https://www.fieldtriptoolbox.org/). 

Specifically, it is derived from the `template/headmodel/standard_bem.mat` file found within the core FieldTrip repository.
